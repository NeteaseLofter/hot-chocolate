{
  "name": "sandbox",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "setup": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build --include-dependencies"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
