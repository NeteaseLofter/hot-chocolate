{
  "name": "sandbox",
  "version": "0.1.4",
  "description": "",
  "scripts": {
    "setup": "pnpm install",
    "version": "changeset",
    "build": "pnpm run build -r",
    "test": "pnpm test -r"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@changesets/cli": "^2.14.1"
  },
  "dependencies": {}
}
