{
  "name": "sandbox",
  "version": "0.1.4",
  "description": "",
  "scripts": {
    "setup": "pnpm install",
    "changeset": "changeset",
    "version": "changeset version",
    "build": "pnpm -r run build",
    "test": "pnpm  -r test",
    "examples": "pnpm --filter ./packages run build  && pnpm --filter ./examples run start"
  },
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@changesets/cli": "^2.14.1"
  },
  "dependencies": {}
}
